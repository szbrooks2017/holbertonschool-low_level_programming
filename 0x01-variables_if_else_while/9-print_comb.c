#include <stdio.h>

/**
 * main - entry point for function
 *
 * Return: returns 0
 */

int main(void)
{

int a;
for (a = 48; a < 58; i++)

{
putchar (a);
if (a == 57)
continue;
putchar (44);
putchar(32);
}
putchar(10);
return (0);
}
